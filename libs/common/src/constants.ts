export const DEFAULT_API_PREFIX = 'api';
export const DEFAULT_API_VERSION = '1';

export const SERVICE_PORTS = {
  apiGateway: 3000,
  identity: 3001,
  sales: 3002,
  inventory: 3003,
  accounting: 3004,
} as const;
